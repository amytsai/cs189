clear all;

x = linspace(-5, 5);
y = linspace(-5, 5);
z = zeros(100,100);
mu1 = [0 2];
mu2 = [
sigma1 = [3 1; 1 2];

for i = 1:100
    for j = 1:100
        z(i,j) = mvnpdf([x(i), y(j)], mu, sigma);
    end
end

contour(x, y, z);